                                         Tabla «evalinservice_schema.evaluation_periods»
     Columna      |               Tipo                | Ordenamiento | Nulable  |                  Por omisión                   
------------------+-----------------------------------+--------------+----------+------------------------------------------------
 id               | uuid                              |              | not null | 
 name             | character varying(200)            |              | not null | 
 description      | text                              |              |          | 
 start_date       | timestamp without time zone       |              | not null | 
 end_date         | timestamp without time zone       |              | not null | 
 questionnaire_id | uuid                              |              | not null | 
 status           | evalinservice_schema.periodstatus |              | not null | 'SCHEDULED'::evalinservice_schema.periodstatus
 created_at       | timestamp without time zone       |              | not null | 
 updated_at       | timestamp without time zone       |              | not null | 
Índices:
    "evaluation_periods_pkey" PRIMARY KEY, btree (id)
    "ix_evaluation_periods_end_date" btree (end_date)
    "ix_evaluation_periods_name" btree (name)
    "ix_evaluation_periods_start_date" btree (start_date)
    "ix_evaluation_periods_status" btree (status)
Restricciones CHECK:
    "max_period_name_length" CHECK (length(name::text) <= 200)
    "min_period_name_length" CHECK (length(name::text) >= 5)
    "valid_date_range" CHECK (end_date > start_date)
Restricciones de llave foránea:
    "evaluation_periods_questionnaire_id_fkey" FOREIGN KEY (questionnaire_id) REFERENCES evalinservice_schema.questionnaires(id)
Referenciada por:
    TABLE "evalinservice_schema.evaluations" CONSTRAINT "evaluations_period_id_fkey" FOREIGN KEY (period_id) REFERENCES evalinservice_schema.evaluation_periods(id)

